# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
*.tsbuildinfo

# worker/tsconfig.json compiles to ../dist (repo root) — Docker-only build output, never committed
/dist/

# Local DB backups (contain real production data — never commit)
backups/